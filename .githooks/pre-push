#!/usr/bin/env bash
# Git pre-push hook — run validation before push
# Skip with: git push --no-verify

set -e

# Find project root (where .git is)
cd "$(git rev-parse --show-toplevel)"

echo "=== Pre-push: Running validation ==="
python3 validate.py